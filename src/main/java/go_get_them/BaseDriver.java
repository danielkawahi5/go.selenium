package go_get_them;

import org.junit.BeforeClass;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;
import org.testng.annotations.BeforeTest;

public class BaseDriver {

    /*public String path = "D:\\IdeaProjects\\go.selenium\\src\\main\\resources\\chromeDriver";
    public String driver = "webdriver.chrome.driver";
    public WebDriver webDiver;

    @BeforeTest
    public void setupDriver() {
        System.setProperty(driver, path);
        webDiver = new ChromeDriver();
        webDiver.manage().window().maximize();

    }*/

}
